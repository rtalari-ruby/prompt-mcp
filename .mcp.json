{
  "_comment_1": "Project-local Claude Code MCP config. Server reads .env from the project root automatically.",
  "_comment_2": "To use globally, copy this entry into ~/.claude.json under mcpServers.",
  "_comment_3": "Make sure to run `npm run build` first so dist/server.js exists.",
  "mcpServers": {
    "prompt": {
      "command": "node",
      "args": ["/Users/rahulraju93/Documents/GitHub/prompt-mcp/dist/server.js"]
    }
  }
}
