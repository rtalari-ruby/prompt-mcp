{
  "_comment_1": "Project-local Claude Code MCP config. Server reads .env from the project root automatically.",
  "_comment_2": "Replace the absolute path below with the path to your local clone.",
  "_comment_3": "Run `npm run build` first so dist/server.js exists.",
  "mcpServers": {
    "prompt": {
      "command": "node",
      "args": ["<ABSOLUTE_PATH_TO_REPO>/dist/server.js"]
    }
  }
}
